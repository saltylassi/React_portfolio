import React from "react";
import styles from "./mainHeader.module.css";

const MainHeader = (props) => {
    return (
        <div className={styles.container}>
            <span className={styles.content}>mainHeader</span>
        </div>
    );
};

export default MainHeader;
